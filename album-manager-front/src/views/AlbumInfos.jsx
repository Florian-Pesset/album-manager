/* import { useEffect } from "react";
import axios from "axios";

export default function AlbumInfos() {


  return (
    <div>
      <div className={styles.card}>
        <img
          src={albumcard.image}
          alt={albumcard.title}
          className={styles.image}
        />
        <p>
          {albumcard.artist} / {albumcard.title}
        </p>
      </div>
    </div>
  );
}

*/
